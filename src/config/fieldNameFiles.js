const oneOrMultipleFile = {
    image: 'image',
    avatars: 'avatars'
}

const singleFile = {
    image: 'image'
}

const multipleFile = {
    avatars: 'avatars'
}

module.exports = {
    oneOrMultipleFile,
    singleFile,
    multipleFile
}
